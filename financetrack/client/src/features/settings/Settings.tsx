import type { User } from '../../services/authService'
type Props = { user: User; onSignOut: () => void }
export default function Settings({ user, onSignOut }: Props) { return <section><div className="page-intro"><p className="eyebrow">SETTINGS</p><h3>Your FinanceTrack</h3><p>Manage account preferences and your data.</p></div><div className="settings-grid"><section className="settings-card"><h2>Profile</h2><div className="profile-row"><span className="profile-avatar">{user.name.charAt(0).toUpperCase()}</span><div><strong>{user.name}</strong><p>{user.email}</p></div></div><label className="form-field">Display name<input defaultValue={user.name} /></label><button className="secondary-button">Save profile</button></section><section className="settings-card"><h2>Preferences</h2><label className="setting-toggle"><span><strong>Monthly summary</strong><small>Receive a spending overview each month.</small></span><input type="checkbox" defaultChecked /></label><label className="setting-toggle"><span><strong>Budget alerts</strong><small>Get notified as limits are approached.</small></span><input type="checkbox" defaultChecked /></label></section><section className="settings-card danger-card"><h2>Session</h2><p>Sign out securely from this device.</p><button className="delete-button" onClick={onSignOut}>Sign out</button></section></div></section> }
